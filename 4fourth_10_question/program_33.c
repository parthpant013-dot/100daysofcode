// Write a program to check if a number is an Armstrong number.

#include <stdio.h>

int main() {
    int n,count;
    printf("Enter any number");
    scanf("%d",&n);
    count =0;
    while (n!=0){
        n=n/10;
        count++;
    }
    
    while(n>0){

    }
    return 0;
}